# NM Meet — pinned runtime dependencies (Python 3.11)

fastapi==0.115.6
uvicorn[standard]==0.34.0

sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10

pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0

# ---- authentication ----
bcrypt==4.2.1
itsdangerous==2.2.0
# Google Sign-In: verifies the id_token signature against Google's public keys.
google-auth==2.37.0
# google.auth.transport.requests needs it to fetch Google's JWKS.
requests==2.32.3

python-dotenv==1.0.1
tzdata==2024.2

# Used by the Google token exchange, and by starlette.testclient.
httpx==0.27.2

# ---- test only; harmless in production, keeps the deploy image consistent ----
pytest==8.3.4
