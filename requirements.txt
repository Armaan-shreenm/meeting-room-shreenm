# NM Meet — pinned runtime dependencies (Python 3.11)

fastapi==0.115.6
uvicorn[standard]==0.34.0

sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10

pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0

# ---- authentication (phase 5) ----
bcrypt==4.2.1
itsdangerous==2.2.0

python-dotenv==1.0.1
tzdata==2024.2

# ---- test only; harmless in production, keeps the deploy image consistent ----
pytest==8.3.4
# Required by starlette.testclient. Not a transitive dependency of anything
# above, so a fresh environment cannot run the suite without it.
httpx==0.27.2
